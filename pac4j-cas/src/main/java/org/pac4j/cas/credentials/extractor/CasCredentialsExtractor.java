package org.pac4j.cas.credentials.extractor;

import org.pac4j.cas.credentials.CasCredentials;
import org.pac4j.core.credentials.extractor.TokenCredentialsExtractor;

/**
 * {@link CasCredentials} extractor.
 *
 * @author Jerome Leleu
 * @since 1.9.2
 */
public interface CasCredentialsExtractor extends TokenCredentialsExtractor<CasCredentials> {
}
